## 2일 _1 문제 풀이

### 1.

```python
maximum = int(data['max_price'])
minimum = int(data['min_price'])

coin_range = maximum - minimum
start_price = int(data['opening_price'])

if start_price + coin_rage > maximum:
    print('상승장')
else:
	print('하락장')
```



### 2.

```python
result = ''
for cha in my_str:
	if cha != 'a' and 'e' and 'i' and 'o' and 'u':
        result += cha
print(result)
```

```python
result = ''
vowels = 'aeiou'
for cha in my_str:
	if cha not in vowels:
		result += cha
print(result)
```



### 3.

```python
fruit_count = 0
not_fruit_count = 0

for fruit, count in basket_itmes.items():
    if fruit in fruits:
        fruit_count += count
    else:
        not_fruit_count += count

print(f'과일은 {fruit_count}개이고, {not_fruit_count}개는 과일이 아닙니다..')
```



### 4.

```python
for i in range(1, len(names)-1):
    short_name = names[i][0] + '.'
    names[i] = short_name
print(' '.join(names))
```



